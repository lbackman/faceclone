<div class="user_<%= user.id %> mb-2">
  <%= link_to user_path(user), options ||= nil do %>
    <%= render_user_avatar(user, avatar_resolution, class: "rounded thumbnail-lg") %>
    <%= user.full_name %>
  <% end %>
</div>
