<% if commentable.comments_count > 0 %>
  <h5>Comments</h5>
  <div id="<%= dom_id(commentable, :comments) %>">
    <% commentable.comments.each do |comment| %>
      <%= render 'comments/comment', comment:, commentable: %>
    <% end %>
  </div>
<% end %>
