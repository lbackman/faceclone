<h1>Likes index</h1>
<%= turbo_frame_tag "#{@type.downcase}_likes_#{params[:id]}" do %>
  <p>Who liked this <%= @type.downcase %>:</p>
  <% @likes.each do |like| %>
    <p><%= link_to like.user.full_name, user_path(like.user), data: { turbo_frame: "_top" } %></p>
  <% end %>
<% end %>
