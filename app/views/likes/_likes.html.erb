<%# turbo_stream_from "likes_#{dom_id(likeable)}" %>
<%= turbo_frame_tag "#{dom_id(likeable, :likes)}" do%>
  <%= render partial: 'likes/modal', locals: { likeable: } %>
  <%= render partial: 'likes/button', locals: { likeable:, liker: } %>
<% end %>
